{
  "name": "@dateline/discovery",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "description": "Dateline Cleveland — patron-discovery SPA (This Week, Then + The Index) + staff Editorial Workbench at /staff. Implemented from the Claude Design converged prototype; the REAL DATA toggle reads apps/pipeline output.",
  "scripts": {
    "export-real": "node --experimental-sqlite scripts/export-real-data.mjs",
    "predev": "npm run export-real",
    "dev": "vite",
    "prebuild": "npm run export-real",
    "build": "tsc -b && vite build",
    "preview": "vite preview"
  },
  "dependencies": {
    "react": "^18.3.1",
    "react-dom": "^18.3.1"
  },
  "devDependencies": {
    "@types/react": "^18.3.12",
    "@types/react-dom": "^18.3.1",
    "@vitejs/plugin-react": "^4.3.4",
    "typescript": "^5.6.3",
    "vite": "^5.4.11"
  }
}
