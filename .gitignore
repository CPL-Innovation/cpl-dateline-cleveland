# secrets
.env
.env.*
!.env.example

# macOS Finder metadata — never wanted, and this repo is public
.DS_Store

# dependencies & build output
node_modules/
apps/*/dist/
apps/*/tsconfig.tsbuildinfo

# pipeline SQLite store (prototype; PD but machine-derived — kept out of git)
**/data/*.sqlite
**/data/*.sqlite-*

# SLICE-07 catalog harvest resume-cache (pointer→pageCount). Just a re-run
# accelerator; the resolved counts are baked into the committed catalog.json.
apps/pipeline/harvest/catalog-pages.json

# pipeline inputs & scratch artifacts
apps/pipeline/inbox/*.jpg
apps/pipeline/inbox/*.jpeg
apps/pipeline/inbox/*.png
apps/pipeline/out/
apps/pipeline/scratch/

# SLICE-03 Scene bake-off — Scene is IN-COPYRIGHT (CC BY-NC-ND, © Northeast Scene).
# The ENTIRE working dir is withheld from the public repo: page images, raw
# transcriptions, scored outputs, gold set — anything Scene-derived. Only generic
# CODE (the adapter, the runner, the scorer) is committable, and it lives in src/.
# This is the commit-allowlist rule (BUILD-SPEC divergence #5) as an operational
# constraint for the first time. See build/SLICE-03-scene.md §RIGHTS GATE.
apps/pipeline/scene-eval/
